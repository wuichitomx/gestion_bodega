# Nunca subir el archivo con las claves reales
.env

# Archivos temporales de Python
__pycache__/
*.pyc

# Archivo temporal de ventas del día (se genera al usar la app, no es código)
ventas_diarias_temp.csv
.venv/
streamlit/secrets.toml 